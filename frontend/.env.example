# ================================
# FRONTEND ENVIRONMENT VARIABLES
# ================================
# Copy this file to .env in the same directory (frontend/.env)
# NEVER commit the .env file to version control!

# ⚠️  IMPORTANT: All React environment variables MUST start with REACT_APP_
# This is a requirement of Create React App / React Scripts

# ================================
# API CONFIGURATION
# ================================

# Backend API URL
# This is where your Express backend server is running
# The frontend will make all API calls to this URL
#
# Local Development: http://localhost:5000
# Production: Replace with your deployed backend URL (e.g., https://api.yourdomain.com)
#
REACT_APP_BACKEND_URL=http://localhost:5000

# ================================
# GOOGLE OAUTH 2.0 CONFIGURATION
# ================================

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/
#
# ⚠️  IMPORTANT: This MUST be the SAME Client ID you configured in backend/.env
#
# SETUP STEPS:
# 1. Go to https://console.cloud.google.com/
# 2. Select your project (same one used for backend)
# 3. Go to "APIs & Services" -> "Credentials"
# 4. Find your OAuth 2.0 Client ID
# 5. Copy the Client ID (it looks like: 123456789-abcdefg.apps.googleusercontent.com)
# 6. Make sure these are configured in the OAuth client settings:
#    - Authorized JavaScript origins: http://localhost:3000
#    - Authorized redirect URIs: http://localhost:3000
#
# Example: 123456789-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com
REACT_APP_GOOGLE_CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# ================================
# ADDITIONAL NOTES
# ================================

# Port Configuration (Optional)
# React runs on port 3000 by default
# To change it, uncomment and modify:
# PORT=3000

# ================================
# TROUBLESHOOTING
# ================================

# If Google OAuth is not working, check:
# 1. CLIENT_ID in backend/.env matches REACT_APP_GOOGLE_CLIENT_ID here
# 2. Authorized JavaScript origins includes http://localhost:3000
# 3. OAuth consent screen is properly configured
# 4. Google+ API is enabled in your Google Cloud project
# 5. You're using the Client ID, not the Client Secret

# If API calls are failing, check:
# 1. Backend server is running on the port specified in REACT_APP_BACKEND_URL
# 2. CORS is properly configured in backend (server.js)
# 3. No typos in the URL (include http:// or https://)

# ================================
# PRODUCTION DEPLOYMENT
# ================================

# When deploying to production:
# 1. Update REACT_APP_BACKEND_URL to your production API URL
# 2. Update REACT_APP_GOOGLE_CLIENT_ID if using a different OAuth client for production
# 3. Make sure your production domain is added to Google OAuth authorized origins
# 4. Always use HTTPS in production URLs
#
# Example production values:
# REACT_APP_BACKEND_URL=https://api.yourdomain.com
# REACT_APP_GOOGLE_CLIENT_ID=production-client-id.apps.googleusercontent.com
