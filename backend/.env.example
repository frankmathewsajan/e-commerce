# ================================
# BACKEND ENVIRONMENT VARIABLES
# ================================
# Copy this file to .env in the same directory (backend/.env)
# NEVER commit the .env file to version control!

# ================================
# DATABASE CONFIGURATION
# ================================

# MongoDB Connection String
# Get this from: https://www.mongodb.com/
# 
# OPTION 1 - MongoDB Atlas (Cloud - Recommended for Production):
# 1. Create a free account at https://www.mongodb.com/cloud/atlas/register
# 2. Create a new cluster (free tier M0 available)
# 3. Click "Connect" -> "Connect your application"
# 4. Copy the connection string and replace <password> with your database user password
# Example: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/ecommerce?retryWrites=true&w=majority
#
# OPTION 2 - Local MongoDB Installation:
# 1. Install MongoDB Community Edition from https://www.mongodb.com/try/download/community
# 2. Start MongoDB service on your machine
# 3. Use: mongodb://localhost:27017/ecommerce
#
MONGO_URI=mongodb://localhost:27017/ecommerce

# ================================
# JWT AUTHENTICATION
# ================================

# JWT Secret Key for signing tokens
# CRITICAL: Use a strong, random string for security!
# Generate using one of these methods:
#
# Method 1 - Node.js (run in terminal):
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# Method 2 - OpenSSL (run in terminal):
# openssl rand -hex 64
#
# Method 3 - Online generator:
# https://www.grc.com/passwords.htm (use "63 random alpha-numeric characters")
#
# Must be at least 32 characters long for security
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long_change_this

# ================================
# EMAIL CONFIGURATION
# ================================

# Gmail Email Address
# The email address from which confirmation emails will be sent
# Example: youremail@gmail.com
EMAIL=your_email@gmail.com

# Gmail App Password (NOT your regular Gmail password!)
# Get this from: https://myaccount.google.com/apppasswords
#
# IMPORTANT STEPS:
# 1. Enable 2-Step Verification on your Google Account first
#    Go to: https://myaccount.google.com/security
# 2. Then go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" as the app and select your device
# 4. Click "Generate" and copy the 16-character password (it will have spaces)
# 5. Paste it here WITHOUT SPACES (example: abcd efgh ijkl mnop -> abcdefghijklmnop)
#
APP_PASSWORD=your_16_character_app_password_no_spaces

# Sender Name (appears in email "From" field)
# Example: E-Commerce Team, YourCompany Support, etc.
NAME=E-Commerce System

# ================================
# GOOGLE OAUTH 2.0 CONFIGURATION
# ================================

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/
#
# SETUP STEPS:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable Google+ API:
#    - Go to "APIs & Services" -> "Library"
#    - Search for "Google+ API" and enable it
# 4. Create OAuth credentials:
#    - Go to "APIs & Services" -> "Credentials"
#    - Click "Create Credentials" -> "OAuth client ID"
#    - Configure consent screen if prompted (use "External" for testing)
#    - Choose "Web application" as application type
# 5. Configure Authorized origins and redirect URIs:
#    - Authorized JavaScript origins: 
#      * http://localhost:3000
#      * http://localhost:5000 (for backend)
#    - Authorized redirect URIs: 
#      * http://localhost:3000
#      * http://localhost:3000/auth/google/callback
# 6. Copy the Client ID (looks like: 123456789-abcdefg.apps.googleusercontent.com)
#
# Example: 123456789-abcdefghijklmnopqrstuvwxyz.apps.googleusercontent.com
CLIENT_ID=your_google_oauth_client_id.apps.googleusercontent.com

# ================================
# CORS CONFIGURATION
# ================================

# Frontend URL for CORS
# This should match where your React app is running
# Local development: http://localhost:3000
# Production: https://yourdomain.com
REACT_APP_FRONTEND_URL=http://localhost:3000

# Backend URL (used in image paths)
# This is where your Express server is running
# Local development: http://localhost:5000
# Production: https://api.yourdomain.com
REACT_APP_BACKEND_URL=http://localhost:5000

# ================================
# SERVER CONFIGURATION (Optional)
# ================================

# Port for backend server (default: 5000)
# Uncomment to change default port
# PORT=5000

# Node Environment
# Options: development, production, test
# NODE_ENV=development

# ================================
# SECURITY NOTES
# ================================
# ⚠️  NEVER commit your .env file to Git!
# ⚠️  Add .env to your .gitignore file
# ⚠️  Use strong, unique values for JWT_SECRET (min 32 chars)
# ⚠️  Use Gmail App Passwords, NOT your actual Gmail password
# ⚠️  Keep your MongoDB credentials secure
# ⚠️  Rotate secrets regularly in production
# ⚠️  Use different credentials for dev/staging/production
# ⚠️  Consider using secret management services in production:
#     - AWS Secrets Manager
#     - Azure Key Vault
#     - HashiCorp Vault
